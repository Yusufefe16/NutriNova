import React from 'react';
import { View, Text } from 'react-native';
import styles from './CustomButton.style.tsx';

const CustomButton = () => {
  return (
    <View style={styles.container}>
      <Text>CustomButton</Text>
    </View>
  );
};

export default CustomButton;
