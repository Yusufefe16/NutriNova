import React from 'react';
import { View, Text } from 'react-native';
import styles from './MealPlanner.style.tsx';

const MealPlanner = () => {
  return (
    <View style={styles.container}>
      <Text>MealPlanner</Text>
    </View>
  );
};

export default MealPlanner;
